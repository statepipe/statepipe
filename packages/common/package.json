{
  "private": true,
  "name": "@statepipe/common",
  "version": "0.1.0",
  "description": "statepipe common scripts",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prettier": "npx prettier -u --check --loglevel=warn .",
    "prettier:fix": "npx prettier --loglevel=warn --write .",
    "eslint": "eslint ./*.js",
    "lint": "npm run eslint && npm run prettier",
    "jest": "node --experimental-vm-modules node_modules/.bin/jest",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/jest --watch",
    "pretest": "npm run lint",
    "test": "npm run jest"
  },
  "author": "igor almeida",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.8",
    "eslint": "^7.16.0",
    "eslint-config-defaults": "^9.0.0",
    "jest": "^26.6.3",
    "prettier": "2.2.1"
  }
}
